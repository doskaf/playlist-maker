<h3>All Playlists</h3>
<% @playlists.each do |playlist| %>
    <li><%= link_to playlist.name, playlist_path(playlist) %> by <%= link_to playlist.user.username, user_path(playlist.user) %></li><br><br>
<% end %>

<%= link_to "View Playlists by Category", playlists_by_category_path %>