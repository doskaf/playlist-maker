<h3><%= @user.username %>'s Profile</h3><br>

<%= link_to "View #{@user.username}'s Playlists", user_playlists_path(@user) %><br><br>

<% if correct_user? %>
  <%= form_tag user_path(@user.id), method: "delete" do %>
    <%= submit_tag "Delete #{@user.username}" %>
  <% end %>
<% end %>