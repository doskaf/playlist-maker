<h3><%= @category.name %></h3><br>

<%= link_to "Create a new #{@category.name} playlist", new_category_playlist_path(@category) %><br><br>

<h4>This category currently has <%= pluralize(@category.playlists.count, "playlist") %>:</h4>

<% @category.playlists.each do |playlist| %>
    <li><%= link_to playlist.name, playlist_path(playlist) %> by <%= link_to playlist.user.username, user_path(playlist.user) %></li>
<% end %>